# BaseConversions
application to convert a decimal of base 10 to another base and vice versa, also added color struct as example for hex 
Has the options to convert from an abritury base from base 10 and a fixed converter for hex, simply since 10-15 is A-F in hex
Example project showcases all options and includes a color struct example to convert hex color to r g b a int values
